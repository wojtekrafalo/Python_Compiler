class Command:

    def __init__(self, command_type, command_data):
        pass
        # variables = []
        # arrays = []
        # self.memory = 0
        #
        # for var in variable_list:
        #     if len(var) == 1:
        #         variables.append(("var", var[0], self.memory))
        #         self.memory += 1
        #     elif len(var) == 3:
        #         first = var[1]
        #         second = var[2]
        #         mem_size = second - first + 1
        #         mem = []
        #         for a in range(mem_size):
        #             mem.append(self.memory)
        #             self.memory += 1
        #
        #         arrays.append(("arr", var[0], mem))
        # print(arrays, variables)
        # self.arrays = arrays
        # self.variables = variables
